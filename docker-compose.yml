services:
  netscan:
    build: .
    container_name: netscan
    ports:
      - "22222:22222"
    volumes:
      - ./data:/app/data
    cap_add:
      - NET_RAW
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
